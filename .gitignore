# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.uv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Secrets — NEVER commit App Store Connect keys or API creds
*.p8
AuthKey_*.p8
.env
.env.*
!.env.example
.logfire/
logfire_credentials.json

# Session-scoped App Store Review Guidelines cache (fetched live, never committed — avoids stale snapshots + redistributing Apple's text)
.asc_cache/
*.guidelines.cache
*.guidelines.json

# OS / editor
.DS_Store
.idea/
.vscode/
